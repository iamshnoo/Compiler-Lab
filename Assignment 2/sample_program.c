/* A sample program passed as input for lexical analyser.*/

#include <stdio.h>

int main()
{
  int a, b, sum;
  double c = 19.67;
  printf("\nEnter two no: ");
  scanf("%d %d", &a, &b);
  sum = 123a + b;
  printf("Sum : %d", sum);
  return 0;
}
